{
  "name": "kotak-research-report-html",
  "version": "1.0.0",
  "description": "HTML template + JSON contract for converting Kotak PCG research one-pager PDFs into an embeddable block on kotakneo.com, published via Strapi.",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "node tools/build.js",
    "build:app": "node tools/build-app.js",
    "render": "node src/render.js",
    "audit": "node tools/audit.js",
    "test": "node tools/test.js",
    "verify:app": "node tools/verify-app.js"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
